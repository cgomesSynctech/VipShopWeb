@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Assim que a Home tenta carregar, ela joga o usuário imediatamente para a tela de login
        Navigation.NavigateTo("/login");
    }
}
